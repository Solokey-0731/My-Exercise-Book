#include<bits/stdc++.h>
// Binary Search (Monotonicity)
/* int l,r,mid;
while(l<r)
{
    if(???)r=mid;// cut down the 
    else l=mid+1;
    mid=(l+r)>>1;
}
// C++ lower_bound(the first value >=val)、 upper_bound
// lower_bound(first,last,val,comp)(compare rule)*/
