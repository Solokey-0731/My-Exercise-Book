#include<bits/stdc++.h>
using namespace std;
// Break and Continue are restricted Goto Statements
// Only used in Circulation and Switch

// The expanding For Statements
// for(t1=str1,t2=str2;*t1&&*t2;t1++,t2++)
// Initial Value can be two
// *t1!='\0'&&*t2!='\0'

// Row Pointer
// int a[m][n];
// a=&(&a[m][0])=&a[0];
// a, a[0]...a[m] are Constant Pointer
// a pointer to an array: T (*p)[n];
// *a[i]=*(a[i])=a[i][0]
// (*a)[i]=a[0][i]


