#include<bits/stdc++.h>
using namespace std;
// Primary Operations:
// s.empty(); empty: return 1
// s.size();  the number of elements
// s.top(); return the top element's value but does'nt delete it
// s.pop(); delete thr top element but doesn't return its value
// s.push(); push a new element on the top of stack

// Based on Array
/*
int main()
{
    stack<int>mystack;
    int sum=0;
    for(int i=0;i<=10;++i) mystack.push(i);
    cout<<"size is "<<mystack.size()<<endl;//size is 11
    while(!mystack.empty())
    {
        cout<<" "<<mystack.top();
        mystack.pop();
    }
    cout<<endl;
    system("pause");
    return 0;
}*/

// Based on Linklist(need Class)
